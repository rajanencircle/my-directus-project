# directus-datepicker

Directus price input with Spanish dates & times.

## Installation

```bash
pnpm add @altipla/directus-extension-datepicker@VERSION
```

Replace `VERSION` with the version you want to install.
