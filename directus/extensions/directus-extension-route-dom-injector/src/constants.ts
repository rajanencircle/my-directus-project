/**
 * constants.ts
 *
 * Shared constants used across multiple modules.
 * Keeping these here prevents circular imports between observer.ts and domActions.ts.
 */

/** Prefix for all console messages from this extension. */
export const LOG = "[route-dom-injector]";
