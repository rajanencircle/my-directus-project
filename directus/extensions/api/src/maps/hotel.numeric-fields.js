// Fields in the hotels schema that are stored as strings but must be cast to numbers.
// Schema inspection (2026-05-18) found all numeric fields are properly typed (integer/decimal).
// No string-encoded number fields were identified.
export const HOTEL_NUMERIC_FIELDS = [];
