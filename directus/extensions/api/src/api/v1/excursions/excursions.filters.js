import { excursions } from "../../shared/collectionFilters.js";

export const {
  buildListFilter,
  buildSort,
  buildIdFilter,
  buildPublicationDeepFilter,
  buildUpdatedAfterFilter,
} = excursions;
