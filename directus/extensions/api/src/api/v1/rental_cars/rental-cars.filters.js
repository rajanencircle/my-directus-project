import { rentalCars } from "../../shared/collectionFilters.js";

export const {
  buildListFilter,
  buildSort,
  buildIdFilter,
  buildPublicationDeepFilter,
  buildUpdatedAfterFilter,
} = rentalCars;
